mixin pagination(options = {})
  -
    const {
      label = '',
      modifier = '',
      addClass = '',
      isLabeled,
    } = options;

    const CLASS_COMPONENT = 'pagination'
    let className = CLASS_COMPONENT
    if(modifier){
      className += ` ${CLASS_COMPONENT}_${modifier}`
    }
    if(addClass){
      className += ` ${addClass}`
    }

  div(class=className)
    - if(!isLabeled){
        p(class=`${CLASS_COMPONENT}__header`)= label
    -}
    
    div(class=`${CLASS_COMPONENT}__links`)
      //- a(class=`${CLASS_COMPONENT}__prev` href='https://change-me' aria-label='пагинация страницы - стрелка назад' title='предыдущая страница')
      //-   img(class=`${CLASS_COMPONENT}__prev-img` src='assets/images/pagination/img/arrow-forward.svg', alt='prev')
      span(class=`${CLASS_COMPONENT}__current`) 1
      a(class=`${CLASS_COMPONENT}__page-numbers` href='https://change-me' aria-label='пагинация страницы 2') 2
      a(class=`${CLASS_COMPONENT}__page-numbers` href='https://change-me' aria-label='пагинация страницы 3') 3
      span(class=`${CLASS_COMPONENT}__dots`) ...
      a(class=`${CLASS_COMPONENT}__page-numbers` href='https://change-me' aria-label='пагинация страницы 15') 15
      a(class=`${CLASS_COMPONENT}__next` href='https://change-me' aria-label='пагинация страницы - стрелка дальше' title='следующая страница')
        img(class=`${CLASS_COMPONENT}__next-img` src=require('./img/arrow-forward.svg'), alt='next')
    p(class=`${CLASS_COMPONENT}__text`) 1 – 12 из 100+ вариантов аренды
    